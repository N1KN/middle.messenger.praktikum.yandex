.LinkButton {
  cursor: pointer;
  color: var(--element-primary-color);
  border: none;
  margin: 0 auto;
  font-size: 11px;
  font-weight: 500;
  line-height: 11px;
  transition: opacity 0.3s;
  display: block;

  &:hover {
    opacity: 0.8;
  }
}
