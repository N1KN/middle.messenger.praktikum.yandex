import Handlebars from 'handlebars';
import { cn } from 'utils/bem';

import './styles.pcss';


const cnProfileTextField = cn('ProfileTextField');

export const ProfileTextField = () => {
    const template = ``;

    return Handlebars.compile(template)({});
}